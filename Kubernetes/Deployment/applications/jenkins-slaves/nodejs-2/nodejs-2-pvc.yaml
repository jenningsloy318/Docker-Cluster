kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nodejs-2
  namespace: jenkins
  annotations:
#    volume.beta.kubernetes.io/storage-class: "devops-k8s-nfs"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
