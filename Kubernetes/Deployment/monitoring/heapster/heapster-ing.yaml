---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
 name: monitoring-heapster
 namespace: kube-system
 annotations:
spec:
 rules:
   - host: heapster.gcsc.sap.corp
     http:
       paths:
         - path: /
           backend:
             serviceName: heapster
             servicePort: 80
