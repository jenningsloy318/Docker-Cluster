apiVersion: extensions/v1beta1
kind: Ingress
metadata:
 name: harbor-ingress
 annotations:
  ingress.kubernetes.io/proxy-body-size: 20480m
  ingress.kubernetes.io/affinity: "cookie"
  ingress.kubernetes.io/session-cookie-name: "harbor"
  ingress.kubernetes.io/session-cookie-hash: "sha"
spec:
 tls:
 - hosts:
   - registry.gcsc.sap.corp
   secretName: registry-secret
 rules:
   - host: registry.gcsc.sap.corp
     http:
       paths:
         - path: /
           backend:
             serviceName: ui
             servicePort: 80
         - path: /v2
           backend:
             serviceName: registry
             servicePort: 5000